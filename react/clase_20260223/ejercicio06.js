const producto = "Camiseta";
const precio = 25.99;
const stock = 12;

console.log(`Producto: ${producto}`);
console.log(`Precio: ${precio} €`);
console.log(`${producto} - ${precio} € (${stock} unidades disponibles)`);