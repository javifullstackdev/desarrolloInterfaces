const estaActivo = true;

const modoOscuro = true;

const esAdmin = true;

const clase1 = estaActivo ? "activo" : "inactivo";
const clase2 = modoOscuro ? "tema-oscuro" : "tema-claro";
const clase3 = esAdmin ? "Administrador" : "Usuario";

